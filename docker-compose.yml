services:
  website:
    image: nginx:alpine
    container_name: kirar00t-site
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
    networks:
      - default

networks:
  default:
    name: nginx_proxy
    external: true
